<configuration scan="true" scanPeriod="1 minute">

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date [%thread] [%file:%line] [%X{}] %highlight(%level) %logger{10} : %msg%n</pattern>
        </encoder>
    </appender>

    <root level="${mitmproxy.logger.level:-INFO}">
        <appender-ref ref="STDOUT"/>
    </root>


</configuration>
